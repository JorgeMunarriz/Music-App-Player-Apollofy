import { FC, FunctionComponentFactory, ReactNode, createContext } from "react";


const QueuePlayerContext = createContext(undefined);


const QueuePlayerProvider: FC<{ children: ReactNode }> = ({ children }) => {






    // return
    // <QueuePlayerContext.Provider value={ }>

    //     {children}

    // </QueuePlayerContext.Provider>


}
